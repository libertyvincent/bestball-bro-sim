# R
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rcheck/
*.rds
*.rda

# renv
renv/library/
renv/local/
renv/cellar/
renv/lock/
renv/python/
renv/staging/

# Build artifacts (CI produces these, never commit)
build/
*.parquet
real_v0_feed/

# Throwaway local diagnostic scripts (e.g. tmp_blend_all.R)
tmp_*.R

# Auto-generated docs from roxygen — regenerate locally via
# devtools::document(). CI uses devtools::load_all() which doesn't need them.
man/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Local-only — published versions live in bestball-bro-data
inst/adp_snapshots/
.Renviron

# Scraped Underdog draft exports (5-10 MB each, local-only inputs for field empirics)
inst/data/scraped_drafts/*.json

# Claude Code per-user settings
.claude/settings.local.json
